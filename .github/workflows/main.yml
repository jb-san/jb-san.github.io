name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Deploy blog
      run: npm run deploy
    - name: Run a multi-line script
      run: |
        npm install
        npm run deploy
